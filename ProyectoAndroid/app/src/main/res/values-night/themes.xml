<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Hereda de tu tema base DayNight -->
    <style name="Base.Theme.ProyectoBussinesOne" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Color de la AppBar en modo oscuro -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- Color de la barra de estado -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <!-- Para compatibilidad en API21+ -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- Color de contenido (iconos, texto) sobre primary -->
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
    </style>
</resources>